class Solution:
    def count(self, head, key):
        curr = head
        count = 0
        while curr != None:
            if curr.data == key:
                count += 1
            curr = curr.next
            
        return count
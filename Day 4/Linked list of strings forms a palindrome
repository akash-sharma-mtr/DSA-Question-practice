def compute(head): 
    curr = head
    s = ''
    while curr != None:
        s += curr.data
        curr = curr.next
        
    if s == s[::-1]:
        return True
    else :
        return False